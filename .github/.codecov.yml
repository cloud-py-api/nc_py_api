codecov:
  notify:
    after_n_builds: 10

comment:
  require_changes: true
  layout: "diff, files"

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch: off
